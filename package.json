{
  "name": "comandia-cameriere",
  "private": true,
  "version": "18.1.0",
  "scripts": {
    "android": "npx cap add android && node fix-android.js && npx cap sync android && node fix-android.js",
    "sync": "npx cap sync android && node fix-android.js",
    "apri": "node fix-android.js && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
